export class AudioManager {
  play(name: string) {
    void name;
    return Promise.resolve(true);
  }
  stop() {
    return Promise.resolve(true);
  }
  fadeTo(volume: number, ms: number) {
    void volume;
    void ms;
    return Promise.resolve(true);
  }
}
